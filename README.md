QUESTIONS: 

---

### 💡 1. Basic Java Syntax
1. Write a program to read a sentence and print how many uppercase, lowercase, digits, and spaces it contains — **without using any built-in counting functions**.

---

### ➕ 2. Data Types & Operators
2. Using only arithmetic operators (**no loops or strings**), determine whether a number is a **3-digit Armstrong number**.  
3. Write a program that finds the **closest integer to a given double** without using `Math.round()` or similar functions.

---

### 🔀 3. If–Else
4. Given three integers, print which one is **numerically in the middle** (neither max nor min).  
5. Determine whether a given year is a **special leap year**, where:
   - It must be divisible by 4  
   - But not divisible by 100  
   - Unless divisible by 400

---

### 🔁 4. Loops
6. Print all numbers between 1 and N whose **reversed number is also prime**.  
7. Generate a custom sequence:  
   \( T(n) = 2 \cdot T(n-1) - T(n-2) \)  
   with first two terms from the user.

---

### 🔧 5. Methods
8. Create a method that checks whether a number is a **perfect square using only integer math**.  
9. Implement a method that returns the **index of the first non-repeating character** in a string.  
10. Write a method to find the **LCM of three numbers**.

---

### 📊 6. Arrays
11. Without using extra arrays, **rotate an array to the left by K positions**.  
12. From an unsorted array, find the **smallest subarray that, if sorted, makes the entire array sorted**.  
13. Count how many elements are **strictly greater than all elements before them**.

---

### ✂️ 7. Strings
14. Check if one string is a **valid shuffle of two other strings**.  
15. Return the **longest word in a sentence** without using `split()`.  
16. Check if a string becomes a **palindrome by removing at most one character**.

---

### 🧱 8. Basic OOP
17. Create a `Book` class with `title`, `author`, `price`.  
    Write a method that finds the **most expensive book among N objects**.  
18. Create a simple `Point` class and write a method to **calculate the distance between two points**.  
19. Create a `Voter` class and print “**Eligible**” or “**Not eligible**” using an **age check method** for a list of voters.

---

### 📥 9. Input / Output
20. Read 10 integers and print the **three highest distinct values**.  
21. Read a paragraph and print the **top 5 most frequent characters**, ignoring spaces.

---

### 🔁 10. Recursion
22. Recursively print all numbers from N to 1 that are **multiples of 3 OR 5**.  
23. Recursively check if a string is **balanced in parentheses**.  
24. Recursively generate all **unique subsets of an array** (no duplicates allowed).

---

### 🧠 11. Final Mixed-Logic Challenge (Your DSA Readiness Test)
25. Given an array, **rearrange it in-place** so that all **negative numbers appear on the left** and all **positive numbers on the right** — **without changing the relative order** of the elements.

---

QUESTIONS CREATED BY CHATGPT AND COPILOT
